import AppWidgets from "@/components/cos/AppWidgets/AppWidgets";
import AppRenderer from "@/components/AppRenderer";

export default function page() {
  return (
    <div>
      <AppWidgets />
      <AppRenderer />
    </div>
  );
}
